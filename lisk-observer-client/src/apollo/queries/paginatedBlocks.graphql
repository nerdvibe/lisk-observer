query paginatedBlocks($page: Int) {
  lastBlocks(page: $page) {
    data {
      id
      height
      username
      generatorPublicKey
      timestamp
      address
    }
    pagination {
      total
      currentPage
      from
      to
    }
  }
}
